export const CHECKING_INTERVAL = 500;

export const NO_BUTTONS_NOTIFICATION = "No actions available for the task";

export const CHANGE_TYPE_BUTTON_TEXTS: Record<`${boolean}`, string> = {
  'true': "Change to another type",
  'false': "Select type",
};